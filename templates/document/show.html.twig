{% extends '@AcMarcheBottin/layout.html.twig' %}

{% block body -%}
    <div class="card bg-light mb-3">
        <div class="card-header">
            <h3>{{ document.name }}</h3>
        </div>
        <div class="card-body">

            {% include '@AcMarcheBottin/document/_actions.html.twig' %}

            <a href="{{ vich_uploader_asset(document, 'file') }}">
                <i class="fas fa-download"></i> Télécharger
            </a>
            <br/><br/>

            {{ document.description | nl2br }}
        </div>
    </div>
{% endblock %}
