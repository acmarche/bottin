{% extends '@AcMarcheBottin/layout.html.twig' %}

{% block body -%}

    <div class="card bg-lig2ht mb-3">
        <div class="card-header">
            <h3>Gestion du bottin marchois</h3>
        </div>
        <div class="card-body">
            {% for fiche in fiches %}
                <a href="{{ path('bottin_token_show', {'uuid': fiche.token.uuid}) }}">{{ fiche.token.uuid }}</a>  <br/>
            {% endfor %}

        </div>
    </div>

{% endblock %}
