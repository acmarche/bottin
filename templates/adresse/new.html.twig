{% extends '@AcMarcheBottin/layout.html.twig' %}

{% block body -%}

    <div class="card mb-3">
        <div class="card-header">
            <h3>Nouvelle adresse</h3>
        </div>
        <div class="card-body">
            {% include '@AcMarcheBottin/adresse/_form.html.twig' %}
        </div>
        <div class="card-footer">
            <a href="{{ path('bottin_adresse') }}">
                <i class="fas fa-long-arrow-alt-left"></i> Annuler
            </a>
        </div>
    </div>

{% endblock %}
