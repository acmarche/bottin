{% extends '@AcMarcheBottin/front/layout.html.twig' %}

{% block body -%}

    <div class="card">
        <div class="card-header">
            <h3>{{ adresse }}</h3>
        </div>
        <div class="card-body">
            {% include '@AcMarcheBottin/admin/adresse/_form.html.twig' %}
        </div>
        <div class="card-footer">
            <a href="{{ path('bottin_admin_adresse_show', {'id': adresse.id}) }}" class="mt-4">
                <i class="ti ti-arrow-back"></i> Annuler
            </a>
        </div>
    </div>

{% endblock %}
