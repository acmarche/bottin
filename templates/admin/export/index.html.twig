{% extends '@AcMarcheBottin/front/layout.html.twig' %}

{% block body -%}

    <div class="card mb-3">
        <div class="card-header">
            <h3>Exportation</h3>
        </div>
        <div class="card-body">

            <p>
                <a href="{{ path('bottin_admin_export_select') }}">Suivant votre sélection, exporter :</a>
            </p>

            <ul class="list-group">
                <li class="list-group-item">
                    <a href="{{ path('bottin_admin_export_fiches_xls') }}" class="btn btn-primary" role="button">
                        <i class="fas fa-download"></i> Exporter les fiches</a>

                </li>
                <li class="list-group-item">
                    <a href="{{ path('bottin_admin_export_categories_xls') }}" class="btn btn-primary" role="button">
                        <i class="fas fa-download"></i> Exporter l'arborescence de rubriques</a>
                </li>
                <li class="list-group-item">
                    <a href="{{ path('bottin_admin_publipostage') }}" class="btn btn-primary" role="button">
                        <i class="far fa-paper-plane"></i>
                        Publiposter</a>
                </li>
            </ul>

        </div>
    </div>

{% endblock %}
