<!DOCTYPE html>
<html lang="fr">
<head>
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8"/>
    <link href="{{ absolute_url(asset('bundles/acmarchebottin/css/pdf.css')) }}" rel="stylesheet">
    <link href="{{ bootcdn }}" rel="stylesheet">
    <title>{{ fiche.societe }}</title>
</head>
<body>
<div class="container">

    <div class="card">
        <div class="card-header">
            <h3>{{ fiche }}</h3>
        </div>
        <div class="card-body">
            {{ include('@AcMarcheBottin/admin/fiche/identite/_activite.html.twig') }}
            <h4>Contact</h4>
            {{ include('@AcMarcheBottin/admin/fiche/identite/_contact.html.twig') }}
            {{ include('@AcMarcheBottin/admin/fiche/identite/_sociaux.html.twig') }}
            {{ include('@AcMarcheBottin/admin/pdf/fiche/_classement.html.twig') }}
        </div>
    </div>
    <div class="page-breaker"></div>

</div>
</body>
</html>
