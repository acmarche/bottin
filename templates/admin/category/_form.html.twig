{{ form_start(form) }}
{{ form_errors(form) }}
{{ form_row(form.name) }}
{{ form_row(form.description) }}
<div class="row">
    <div class="col-6">{{ form_row(form.iconFile) }}</div>
    <div class="col-6">{{ form_row(form.color) }}</div>
    <div class="col-6">{{ form_row(form.logoFile) }}</div>
    <div class="col-6">{{ form_row(form.logoBlancFile) }}</div>
</div>

{{ form_row(form.mobile) }}
<button class="btn btn-primary mr-1 mb-2" type="submit"><i class="ti ti-device-floppy" aria-hidden="true"></i>
    Sauvegarder
</button>

{{ form_end(form) }}
