{% extends '@AcMarcheBottin/front/layout.html.twig' %}

{% block body -%}

    <div class="card">
        <div class="card-header">
            <h3>{{ category }}</h3>
        </div>
        <div class="card-body">

            {% include '@AcMarcheBottin/admin/category/_form.html.twig' %}

        </div>
        <div class="card-footer">
            <a href="{{ path('bottin_admin_category_show', {'id': category.id}) }}">
                <i class="ti ti-arrow-back"></i> Annuler
            </a>
        </div>
    </div>


{% endblock %}
