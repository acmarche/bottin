{% extends '@AcMarcheBottin/front/layout.html.twig' %}

{% block body -%}
    <div class="card">
        <div class="card-header">
            <h3>Nouvelle catégorie {% if parent %}<strong>dans {{ parent }}</strong> {% endif %}</h3>
        </div>
        <div class="card-body">

            {% include '@AcMarcheBottin/admin/category/_form.html.twig' %}

            <a href="{{ path('bottin_admin_category') }}" class="float-end">
                <i class="fas fa-long-arrow-alt-left"></i> Annuler
            </a>
        </div>
    </div>

{% endblock %}
