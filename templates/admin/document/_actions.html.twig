{% if is_granted('ROLE_BOTTIN_ADMIN') %}
    <a href="{{ path('bottin_document_edit', { 'id': document.id }) }}" class="btn btn-primary"
       role="button"><i class="far fa-edit"></i> Editer</a>

    <a href="{{ path('bottin_fiche_show',{'id': document.fiche.id}) }}" class="btn btn-primary">
        <i class="fas fa-long-arrow-alt-left"></i> Retour à la fiche
    </a>

    <button type="button" class="btn btn-danger" data-toggle="modal" data-target="#deleteModal">
        <i class="fas fa-trash"></i> Supprimer le document
    </button>
    <br/><br/>
{% endif %}
