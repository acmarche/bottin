{{ form_start(form) }}
{{ form_errors(form) }}
    {{ form_row(form.groupe) }}
<div class="row">
    <div class="col-6">{{ form_row(form.name) }}</div>
    <div class="col-6">{{ form_row(form.private) }}</div>
</div>
<div class="row">
    <div class="col-6">{{ form_row(form.iconFile) }}</div>
    <div class="col-6">{{ form_row(form.color) }}</div>
</div>

<button class="btn btn-primary mr-1 mb-2" type="submit"><i class="ti ti-device-floppy" aria-hidden="true"></i>
    Sauvegarder
</button>

{{ form_end(form) }}