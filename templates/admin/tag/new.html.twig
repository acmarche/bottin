{% extends '@AcMarcheBottin/front/layout.html.twig' %}

{% block body -%}

    <div class="card">
        <div class="card-header">
            <h3>Nouveau tag</h3>
        </div>
        <div class="card-body">
            {% include '@AcMarcheBottin/admin/tag/_form.html.twig' %}
        </div>
        <div class="card-footer">
            <a href="{{ path('bottin_admin_tag') }}">
                <i class="ti ti-arrow-back"></i> Annuler et retour à la liste
            </a>
        </div>
    </div>

{% endblock %}
