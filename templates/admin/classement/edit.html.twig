{% extends '@AcMarcheBottin/front/layout.html.twig' %}

{% block body -%}

    <div class="card">
        <div class="card-header">
            <h3>Classements pour
                <a href="{{ path('bottin_admin_fiche_show', { 'id': fiche.id }) }}">
                    {{ fiche }}
                </a>
            </h3>
        </div>

        <div class="card-body">
{% include '@AcMarcheBottin/backend/classement/_form.html.twig' %}
        </div>
    </div>
{% endblock %}
