{% extends '@AcMarcheBottin/front/layout.html.twig' %}

{% block body -%}

    <div class="card">
        <div class="card-header">
            <h3>Publipostage</h3>
        </div>
        <div class="card-body">
            <p>
                <a href="{{ path('bottin_admin_export_select') }}"><i class="ti ti-check-square"></i>
                    Suivant la sélection que vous avez effectué</a> :
                ({{ fiches|length }} fiches)
            </p>

            <ul class="list-group">
                <li class="list-group-item">
                    <a href="{{ path('bottin_admin_publipostage_mail_all') }}">Envoyer par mail</a>
                </li>
                <li class="list-group-item">
                    <a href="{{ path('bottin_admin_publipostage_paper_all') }}">Générer un pdf pour impression</a>
                    <br />
                    <span class="text-muted">Le traitement est long (4 minutes), merci d'être patient ;-)</span>
                </li>
            </ul>

        </div>
    </div>

{% endblock %}
