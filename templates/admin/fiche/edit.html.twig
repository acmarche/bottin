{% extends '@AcMarcheBottin/front/layout.html.twig' %}

{% block body -%}
    <div class="card mb-3">
        <div class="card-header">
            <h3>{{ fiche }}</h3>
            {% if fiche.ftlb %}
                <img src="{{ asset('bundles/acmarchebottin/images/ftlb.png') }}" title="Fiche provenant de la Ftlb"
                     alt="ftlb"/>
            {% endif %}
        </div>
        <div class="card-body">

            {% include '@AcMarcheBottin/admin/fiche/_form.html.twig' %}

            <a href="{{ path('bottin_admin_fiche_show', {'id' : fiche.id }) }}" class="btn btn-secondary">
             <i class="fas fa-long-arrow-alt-left"></i> Annuler
            </a>
        </div>
    </div>

{% endblock %}
{% block javascripts %}
    {{ include('@AcMarcheBottin/admin/fiche/_horaires.js.html.twig') }}
{% endblock %}
