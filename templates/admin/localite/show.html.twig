{% extends '@AcMarcheBottin/front/layout.html.twig' %}

{% block body -%}
    <div class="card">
        <div class="card-header">
            <h3>{{ localite }}</h3>
        </div>
        <div class="card-body">

            <a href="{{ path('bottin_admin_localite_edit', { 'id': localite.id }) }}" class="btn btn-primary mb-3"
               role="button"><i class="far fa-edit"></i> Editer</a>

            {% include '@AcMarcheBottin/admin/localite/_fiches.html.twig' %}
        </div>
        <div class="card-footer">

        </div>
    </div>
{% endblock %}
