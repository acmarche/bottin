{% extends '@AcMarcheBottin/front/layout.html.twig' %}

{% block body %}
    <div class="card">
        <h3 class="card-header">Les 100 dernières modifications</h3>

        <div class="card-body">
            <table class="table table-bordered">
                <thead>
                <tr>
                    <th>Fiche</th>
                    <th>Par</th>
                    <th>Champ</th>
                    <th>Ancienne valeur</th>
                    <th>Nouvelle valeur</th>
                    <th>Date</th>
                </tr>
                </thead>
                <tbody>
                {% for history in histories %}
                    <tr>
                        <td>
                            <a href="{{ bottin_url_fiche_show(history.fiche) }}">
                                {{ history.fiche }}
                            </a>
                        </td>
                        <td>{{ history.madeBy }}</td>
                        <td>{{ history.property }}</td>
                        <td>{{ history.oldValue }}</td>
                        <td>{{ history.newValue }}</td>
                        <td>{{ history.createdAt | format_datetime('full', 'short') }}</td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>
        </div>
    </div>
{% endblock %}
