{% extends '@AcMarcheBottin/backend/layout.html.twig' %}

{% block body -%}

    <div class="card">
        <div class="card-header">
            <h3>
                Sélectionnez vos secteurs d'activités
                <a href="{{ bottin_url_fiche_show(fiche) }}">
                    {{ fiche }}
                </a>
            </h3>
        </div>

        <div class="card-body">
            {% include '@AcMarcheBottin/backend/classement/_form.html.twig' %}
        </div>
    </div>

{% endblock %}
