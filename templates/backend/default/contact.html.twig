{% extends '@AcMarcheBottin/backend/layout.html.twig' %}

{% block body -%}
    <div class="card">
        <div class="card-header">
            <h3>
                Contactez <a href="https://www.marche.be/economie/bottin/fiche/agence-de-developpement-local-adl/"
                                 target="_blank">L'Agence de Développement Local
                </a>
            </h3>
        </div>
        <div class="card-body">
            {{ form_start(form) }}
            {{ form_errors(form) }}
            {{ form_widget(form) }}
            <button class="btn btn-primary mr-1 mb-2" type="submit"><i class="ti ti-device-floppy" aria-hidden="true"></i>
                Envoyer
            </button>
            {{ form_end(form) }}
        </div>
    </div>

{% endblock %}
