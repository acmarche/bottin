{% extends '@AcMarcheBottin/backend/layout.html.twig' %}

{% block body -%}
    <div class="card mb-3">
        <div class="card-header">
            <h3>{{ fiche }}</h3>
        </div>
        <div class="card-body">
            {{ form_start(form) }}
            {{ form_errors(form) }}

            {% include '@AcMarcheBottin/backend/fiche/form/_activite.html.twig' %}

            <button class="btn btn-primary mr-1 mb-2" type="submit"><i class="far fa-save" aria-hidden="true"></i>
                Suivant
            </button>

            {{ form_end(form) }}
        </div>
    </div>

{% endblock %}
