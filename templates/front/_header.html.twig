<header class="p-3 mb-3 border-bottom">
    <div class="container">

        <div class="d-flex flex-wrap align-items-center justify-content-center justify-content-lg-start">
            <a href="/" class="d-flex align-items-center mb-3 mb-lg-0 me-lg-auto text-dark text-decoration-none">
                <i class="fas fa-store icon-rose"></i> <span class="fs-4">Bottin</span>
            </a>

            <ul class="nav col-12 col-lg-auto me-lg-auto mb-2 justify-content-center mb-md-0" id="topmenu">
                {% include '@AcMarcheBottin/front/_menu.html.twig' %}
            </ul>

            {{ render(url('bottin_search_form')) }}

            {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                <div class="dropdown text-end">
                    <a href="#" class="d-block link-dark text-decoration-none dropdown-toggle" id="dropdownUser1"
                       data-bs-toggle="dropdown" aria-expanded="false">
                        <img src="https://github.com/mdo.png" alt="mdo" width="32" height="32" class="rounded-circle">
                        {{ app.user }}
                    </a>
                    <ul class="dropdown-menu text-small" aria-labelledby="dropdownUser1" style="">
                        {# <li><a class="dropdown-item" href="#">New project...</a></li>
                        <li><a class="dropdown-item" href="#">Settings</a></li>
                        <li><a class="dropdown-item" href="#">Profile</a></li>
                        <li>
                            <hr class="dropdown-divider">
                        </li> #}
                        <li><a class="dropdown-item" href="{{ path('app_logout') }}">Me déconnecter</a></li>
                    </ul>
                </div>
            {% else %}
                <a type="button" class="btn btn-outline-primary me-2" href="{{ path('app_login') }}">Me connecter</a>
            {% endif %}
        </div>
    </div>
</header>
